{
  "name": "cheatatlinkedingames",
  "description": "Cheat at LinkedIn puzzle games (starting with Zip)",
  "private": true,
  "version": "0.0.5",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "prebuild": "npm version patch --no-git-tag-version --allow-same-version",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "wxt": "latest",
    "typescript": "^5.6.0"
  }
}
